# RuoYi AI 系统用例图

## 图2.1 RuoYi AI 系统用例图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#1890ff', 'primaryTextColor': '#fff', 'primaryBorderColor': '#1890ff', 'lineColor': '#333', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fff'}}}%%
graph TB
    subgraph 参与者
        A([👤 游客])
        B([👤 普通用户])
        C([👤 管理员])
    end
    
    subgraph 普通用户功能
        UC1((UC1: 用户登录))
        UC2((UC2: 修改密码))
        UC3((UC3: AI聊天问答))
        UC4((UC4: 语音输入))
        UC5((UC5: 文件上传))
        UC6((UC6: 文件下载))
        UC7((UC7: 文件预览))
        UC8((UC8: 数字人交互))
        UC9((UC9: 会话历史))
    end
    
    subgraph 管理员功能
        UC10((UC10: 用户管理))
        UC11((UC11: 角色管理))
        UC12((UC12: 数字人配置))
        UC13((UC13: 系统配置))
    end
    
    A --> UC1
    UC1 --> B
    
    B --> UC2
    B --> UC3
    B --> UC4
    B --> UC5
    B --> UC6
    B --> UC7
    B --> UC8
    B --> UC9
    
    C --> UC10
    C --> UC11
    C --> UC12
    C --> UC13
    
    classDef participant fill:#1890ff,stroke:#333,color:#fff
    classDef usecase fill:#e6f7ff,stroke:#1890ff,color:#333
    classDef adminUsecase fill:#fff7e6,stroke:#fa8c16,color:#333
    
    class A,B,C participant
    class UC1,UC2,UC3,UC4,UC5,UC6,UC7,UC8,UC9 usecase
    class UC10,UC11,UC12,UC13 adminUsecase
```

### 说明

该用例图展示了RuoYi AI系统的整体功能全景。从图中可以看出，系统主要涉及三类参与者：**游客**、**普通用户**和**管理员**。游客作为系统外部人员，只能进行注册和登录操作；普通用户登录后可使用AI聊天、文件管理、数字人交互等核心业务功能；管理员拥有系统最高权限，可对用户、角色、数字人配置等进行管理。用例图清晰地呈现了不同角色与系统功能之间的对应关系，便于后续需求分析和权限设计。
